export const environment = {
  production: true,
  URL_PATH: '/api', // En producción normalmente las llamadas van al mismo dominio o un proxy
  finanzasServerUrl: 'https://cifrado.finanzaschiapas.gob.mx'
};
